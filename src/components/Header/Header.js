import React from 'react';
// import { Formik, Form, Field } from "formik";
import { HeaderContainer } from './styledHeader';

const Header = () => {
    return (
        <HeaderContainer><p>New Event</p></HeaderContainer>
    );
}
export default Header;